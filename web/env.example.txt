PYSERVER_URL=http://localhost:8001
RUNTIME_DATA_DIR=data/runtime
NEXT_PUBLIC_SITE_URL=http://localhost:3100

# End-of-day control plane. Defaults target the existing local ports and
# /a-share deployment, so only override these for another environment.
DAILY_CLOSE_LOCAL_WEB_URL=http://127.0.0.1:3100
DEPLOY_HOST=root@47.77.231.22
NEXT_BASE_PATH=/a-share
DAILY_CLOSE_REMOTE_BASE_URL=http://47.77.231.22/a-share

# Optional: only needed for AI-assisted universe refresh, not for trading signals.
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_BASE_URL=https://api.deepseek.com
