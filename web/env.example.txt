PYSERVER_URL=http://localhost:8001
RUNTIME_DATA_DIR=data/runtime
NEXT_PUBLIC_SITE_URL=http://localhost:3100

# Self-hosted Umami. Leave blank to disable analytics in local development.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
NEXT_PUBLIC_UMAMI_SCRIPT_URL=/analytics/script.js
NEXT_PUBLIC_UMAMI_RECORDER_URL=/analytics/recorder.js
NEXT_PUBLIC_UMAMI_DOMAINS=47.77.231.22
NEXT_PUBLIC_UMAMI_REPLAY_ENABLED=0

# Protect expensive server-only routes such as ad-hoc backtests and live rescoring.
INTERNAL_API_TOKEN=replace-with-a-long-random-secret

# End-of-day control plane. Defaults target the existing local ports and
# /a-share deployment, so only override these for another environment.
DAILY_CLOSE_LOCAL_WEB_URL=http://127.0.0.1:3100
DEPLOY_HOST=root@47.77.231.22
NEXT_BASE_PATH=/a-share
DAILY_CLOSE_REMOTE_BASE_URL=http://47.77.231.22/a-share

# Optional: only needed for AI-assisted universe refresh, not for trading signals.
# LLM provider switch: "deepseek" (default) or "kimi". Unknown values fail fast.
LLM_PROVIDER=deepseek
# Shared request timeout for whichever provider is active (ms).
LLM_TIMEOUT_MS=120000

# DeepSeek provider (default).
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Kimi provider — only needed when LLM_PROVIDER=kimi. OpenAI-compatible
# endpoint; point KIMI_LLM_BASE_URL at a local Kimi bridge's /v1 root to use
# a bridged desktop session instead of a direct API key.
KIMI_LLM_API_KEY=
KIMI_LLM_BASE_URL=https://api.moonshot.cn/v1
KIMI_LLM_MODEL=kimi-k2-0905-preview

# Kimi data bridge (read-only). Directory where a Kimi Work Automation drops
# CSV/JSON files (screen-results, consensus) after the A-share close. No
# credentials needed on this side; missing or stale files degrade to empty
# results and the normal data path is used. Relative to the web/ working dir.
KIMI_BRIDGE_DIR=data/kimi-bridge
# Refuse bridge files older than this many hours (0 disables the check).
KIMI_BRIDGE_MAX_AGE_HOURS=48

# Optional: only needed by web/scripts/monitor.py.
# Keep the real Feishu chat id in the local environment, never in source.
FEISHU_CHAT=feishu:oc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
