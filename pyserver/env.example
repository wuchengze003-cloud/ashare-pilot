# Copy to pyserver/.env (gitignored) before running.
# Register a free Pro account at https://tushare.pro/register
TUSHARE_TOKEN=your-tushare-pro-token-here

# Optional: set this only for a Tushare-compatible HTTP proxy.
# Leave unset when using the official Tushare endpoint.
TUSHARE_HTTP_URL=
